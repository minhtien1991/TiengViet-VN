ivr = IVR
ivr.class_of_service_id = Lớp của dịch vụ
ivr.class_of_service_id.tooltip = Chọn một lớp dịch vụ cho IVR này. Nếu bạn chọn "Extensions Chỉ", người gọi sẽ chỉ có thể quay số mở rộng nội bộ
ivr.description = miêu tả
ivr.description.tooltip = Mô tả tóm tắt để xác định IVR này.
ivr.disabled = Vô hiệu hóa
ivr.entries = Vé
ivr.entries.destination = Module / Điểm đến
ivr.entries.dest.empty = một giá trị mục tiêu trong IVR là bắt buộc.
ivr.entries.empty = Không có mục định nghĩa IVR
ivr.entries.enabled = kích hoạt
ivr.entries.mod_dest.empty = một giá trị mục tiêu cho các mô-đun IVR là bắt buộc.
ivr.entries.option = chữ số
ivr.entries.option.duplicate = IVR nhập chữ số phải là duy nhất.
ivr.entries.option.invalid = IVR nhập chữ số phải chứa một hoặc nhiều số.
ivr.entries.option.placeholder = quay số bằng chữ số
ivr.entries.option.pound = Phím (#) được dành riêng để hoàn thành cuộc gọi.
ivr.entries.option.star = Dấu (*) để phát lại lời chào
ivr.freedial = gọi trực tiếp
ivr.freedial.tooltip = Kích hoạt tính năng cuộc gọi trực tiếp đến bất kỳ số lượng cho phép của lớp dịch vụ được cấu hình trong IVR này.
ivr.general = Chung 
ivr.invalid_add_msg = Chào mừng Sau Thử lại
ivr.invalid_add_msg.tooltip = Sau khi chơi được thông báo thử lại không hợp lệ, hệ thống sẽ đóng thông điệp chào mừng đến IVR.
ivr.invalid_dest = đích không hợp lệ
ivr.invalid_msg_id = tin nhắn không hợp lệ
ivr.invalid_msg_id.tooltip = Tin nhắn được lắng nghe khi số lượng tối đa nỗ lực đạt được.
ivr.invalid_retry_msg_id = Tin Thử lại không hợp lệ
ivr.invalid_retry_msg_id.tooltip = Thông điệp được nghe khi một tùy chọn không hợp lệ được nhận.
ivr.invalid_tries = Những nỗ lực không hợp lệ
ivr.invalid_tries.tooltip = Số lần tìm cách hợp lệ cho phép.
ivr.mod_invalid = Mô-đun đích không hợp lệ
ivr.mod_timeout = Thời gian chờ tối đa Mô-đun Target
ivr.none = không ai
ivr.only_extensions = chỉ Extensions
ivr.timeout = timeout
ivr.timeout.tooltip = Số tiền tối đa thời gian cho phép sau khi thất bại trong hàng loạt các ưu tiên cho một kênh trong đó người dùng nên bắt đầu gõ một số phần mở rộng.
ivr.timeout_add_msg = Thông điệp chào mừng sau khi hoàn thành thời gian chờ
ivr.timeout_add_msg.tooltip = Sau khi nghe thời gian retry nhắn tối đa, hệ thống sẽ chơi thông điệp chính của IVR hoan nghênh.
ivr.timeout_dest = Không nơi trả lời
ivr.timeout_msg_id = Thông báo cho thời gian chờ
ivr.timeout_msg_id.tooltip = Thông báo cho chơi khi thời gian chờ là đạt.
ivr.timeout_retry_msg_id = Thử lại thông điệp để đạt được tối đa thời gian chờ đợi
ivr.timeout_retry_msg_id.tooltip = Thông điệp rằng đóng khi thời gian chờ là đạt. Sau đó, nó sẽ nhắc người dùng để thử lại.
ivr.timeout_tries = Số timeout nỗ lực
ivr.timeout_tries.tooltip = Số lần cho phép khi không có DTMF nhận được.
ivr.welcome_msg_id = Lời chào mừng
ivr.welcome_msg_id.tooltip = Phát lại thông điệp chào mừng.
ivr.instructions_msg_id = Nhắn hướng dẫn
ivr.instructions_msg_id.tooltip = Thông báo cho sinh sản sau khi thông điệp chào mừng. Thông điệp này rất hữu ích để tránh lặp lại thông điệp chào mừng sau khi tùy chọn vô hiệu hoặc nhập một thời gian dài mà không cần gõ bất kỳ tùy chọn.
menu.ivr = IVR
module.ivr = IVR